[gd_scene format=3 uid="uid://kf4b55q1ly4l"]

[ext_resource type="Texture2D" uid="uid://cujr13kp4sccm" path="res://assets/Hero.png" id="1_p0vlq"]
[ext_resource type="PackedScene" uid="uid://4mvswf083i1n" path="res://scenes/stake.tscn" id="2_3li8b"]

[sub_resource type="GDScript" id="GDScript_v6fml"]
script/source = "extends CharacterBody2D

signal died

const SPEED = 300.0
var is_attacking = false
var hitbox_offset : Vector2
var max_health: int
var health: int
var alive: bool = true

@onready var animated_sprite_2d: AnimatedSprite2D = $AnimatedSprite2D
@onready var damage_cooldown: Timer = $DamageCooldown
@onready var hand = $Hand
@export var ref_stake : PackedScene


#func _ready() -> void:
	#health = player_stats.health
	#max_health = player_stats.max_health


func _physics_process(_delta: float) -> void:
	if alive:
		if Input.is_action_just_pressed(\"attack\") and not is_attacking:
			attack()
		if is_attacking:
			velocity = Vector2.ZERO
			return
		process_movement()
		move_and_slide()

func process_movement() -> void:
	var direction := Input.get_vector(\"left\", \"right\", \"up\", \"down\")
	velocity = direction * SPEED
	play_animation(direction)

func process_animation() -> void:
	if is_attacking:
		return

func play_animation(dir: Vector2) -> void:
	if dir.x != 0:
		animated_sprite_2d.flip_h = dir.x < 0
		animated_sprite_2d.play(\"idle\")
	if dir.y < 0:
		animated_sprite_2d.play(\"idle\")
	if dir.y > 0:
		animated_sprite_2d.play(\"idle\")

func attack() -> void:
	print(\"pew\")
	is_attacking = true
	animated_sprite_2d.play(\"attack\")
	if ref_stake:
		var stake = ref_stake.instantiate()
		get_tree().current_scene.add_child(stake)
		stake.global_position = self.global_position
		var stake_rotation = self.global_position.direction_to(get_global_mouse_position()).angle()
		stake.rotation = stake_rotation
		
func _on_animated_sprite_2d_animation_finished() -> void:
	if is_attacking:
		is_attacking = false
	animated_sprite_2d.play(\"idle\")


func _on_hitbox_body_entered(body: Node2D) -> void:
	print(\"hit\")

func take_damage(amount: int) -> void:
	if alive:
		if damage_cooldown.time_left > 0:
			return
		health -= amount
		#player_stats.health = health
		if health <= 0:
			die()
		damage_cooldown.start()

func die() -> void:
	alive = false
	$Hero.hide()
	died.emit()
	
"

[sub_resource type="AtlasTexture" id="AtlasTexture_ij6vw"]
atlas = ExtResource("1_p0vlq")
region = Rect2(0, 0, 32, 32)

[sub_resource type="AtlasTexture" id="AtlasTexture_ghijl"]
atlas = ExtResource("1_p0vlq")
region = Rect2(32, 0, 32, 32)

[sub_resource type="AtlasTexture" id="AtlasTexture_44cfe"]
atlas = ExtResource("1_p0vlq")
region = Rect2(64, 0, 32, 32)

[sub_resource type="SpriteFrames" id="SpriteFrames_dd465"]
animations = [{
"frames": [{
"duration": 1.0,
"texture": SubResource("AtlasTexture_ij6vw")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_ghijl")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_44cfe")
}],
"loop": true,
"name": &"idle",
"speed": 7.0
}, {
"frames": [],
"loop": true,
"name": &"new_animation",
"speed": 5.0
}]

[node name="Player" type="CharacterBody2D" unique_id=1825044717]
script = SubResource("GDScript_v6fml")
ref_stake = ExtResource("2_3li8b")

[node name="AnimatedSprite2D" type="AnimatedSprite2D" parent="." unique_id=356846681]
scale = Vector2(4, 4)
sprite_frames = SubResource("SpriteFrames_dd465")
animation = &"new_animation"
autoplay = "idle"
